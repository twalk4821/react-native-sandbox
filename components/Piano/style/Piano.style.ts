import {
  StyleSheet,
} from 'react-native';

const style = StyleSheet.create({
  container: {
    width: 100,
    height: 100
  },
  key: {
    width: 10,
    height: 10,
    backgroundColor: 'green'
  }
});

export default style;